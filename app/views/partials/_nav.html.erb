
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" id="white"href="#">Yung Lamb</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li id="white">
          <%=link_to 'Posts', posts_path, :id => "white"%>
        </li>
        <% if user_signed_in? %>
        <li id="white"><%= link_to "Sign out", destroy_user_session_path, :method => :delete, :id => "white" %></li>
        <% else %>
        <li id="white"><%= link_to 'Sign In', new_user_session_path, :id => "white"%></li>
        <li id="white"><%= link_to 'Register', new_user_registration_path, :id => "white" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
